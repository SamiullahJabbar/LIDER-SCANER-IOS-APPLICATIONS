name: lidar_scanner_app
description: "LiDAR Pro Scanner - Professional 3D Scanning App"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter

  # UI
  cupertino_icons: ^1.0.8
  
  # ARKit for LiDAR scanning (iOS only)
  arkit_plugin: ^1.2.0
  
  # 3D model viewing
  model_viewer_plus: ^1.9.2
  
  # HTTP client for API calls
  dio: ^5.4.0
  
  # Local storage
  sqflite: ^2.4.1
  path_provider: ^2.1.1
  
  # State management
  provider: ^6.1.1
  
  # File handling
  path: ^1.9.1
  
  # Encryption
  encrypt: ^5.0.3
  
  # Permissions
  permission_handler: ^12.0.1
  
  # UUID generation
  uuid: ^4.2.2
  
  # JSON serialization
  json_annotation: ^4.9.0
  
  # Onboarding
  smooth_page_indicator: ^2.0.1
  
  # Animations
  lottie: ^3.2.0
  animate_do: ^4.2.0
  
  # Icons
  flutter_svg: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  
  # Code generation for JSON
  build_runner: ^2.4.13
  json_serializable: ^6.8.0
  
  # App icon generator
  flutter_launcher_icons: ^0.14.4

# Flutter Launcher Icons Configuration
flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icons/icon.png"
  adaptive_icon_background: "#0A2463"
  adaptive_icon_foreground: "assets/icons/icon.png"

flutter:
  uses-material-design: true

  assets:
    - assets/icons/
