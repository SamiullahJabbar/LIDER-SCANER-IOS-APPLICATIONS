workflows:
  my-workflow:
    name: Flutter Build
    environment:
      flutter: stable
    scripts:
      - name: Build APK
        script: flutter build apk --debug
    artifacts:
      - build/app/outputs/flutter-apk/app-debug.apk
